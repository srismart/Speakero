services:
  - type: web
    name: speakero
    runtime: docker
    plan: starter
    healthCheckPath: /
    envVars:
      - key: SMALLEST_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
